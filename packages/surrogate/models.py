"""Neural operator surrogates: DeepONet baseline first, then temporal Graph-FNO (GNN encoder + FNO
    along the 24 h axis).

Stage: S3 (W8-W9)
Gate served: S3 - < 2% rel. L2 held-out, measured speedup vs JAX core, gradient agreement

Stage 0 placeholder: no implementation yet.
"""
