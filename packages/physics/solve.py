"""Newton-Raphson steady-state solve with implicit differentiation via jax.lax.custom_root (never
    unrolled).

Stage: S1 (W3-W4)
Gate served: S1 - gradients vs central finite differences < 1e-4; 1,000 scenarios converge with
    0 failures

Stage 0 placeholder: no implementation yet.
"""
